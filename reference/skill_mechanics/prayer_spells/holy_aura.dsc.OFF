holyaura_task:
    type: task
    definitions: caster
    debug: true
    script:
    - define level <[caster].flag[skill.spells.holyaura.level]>
    - flag <[caster]> double_damage_mod:5
    - repeat <[level].mul[2]>:
        - wait 5t
        # - playeffect at:<[caster].location.forward[2].above[1]> effect:note offset:0 special_data:2|<color[255,0,0]> data:1
        # - define Angle <location[0,0,1].rotate_around_y[<[value].to_radians.mul[<[Denisity]>]>]>
        # - playeffect effect:note at:<[Loc].add[<[Angle]>]>
        - playeffect redstone <[value]> special_data:1|238,221,151 visibility:10 quantity:<[value]>

    - flag <[caster]> double_damage_mod:!